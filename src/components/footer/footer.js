import { Component } from "preact";

// Footer bar for the app

export default class Footer extends Component {
  render() {
    return (
      <footer class="footer">
        <div class="footer__container">Footer</div>
      </footer>
    );
  }
}

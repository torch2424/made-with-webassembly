{
  "private": true,
  "name": "made-with-webassembly",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "run-s showcase:build preact:build",
    "dev": "run-s preact:dev",
    "lint": "eslint src",
    "serve": "preact serve",
    "showcase:build": "node showcase-build.js",
    "preact:dev": "preact watch",
    "preact:build": "preact build"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^4.5.0",
    "eslint-config-synacor": "^1.1.0",
    "if-env": "^1.0.0",
    "mkdirp": "^0.5.1",
    "npm-run-all": "^4.1.5",
    "preact-cli": "^2.0.0",
    "recursive-copy": "^2.0.10",
    "recursive-readdir": "^2.2.2",
    "showdown": "^1.9.1"
  },
  "dependencies": {
    "fuse.js": "^3.4.5",
    "preact": "^8.5.2",
    "preact-compat": "^3.17.0",
    "preact-render-to-string": "^4.1.0",
    "preact-virtual-list": "^0.3.1"
  }
}
